import { FromlabelhighlightDirective } from './fromlabelhighlight.directive';

describe('FromlabelhighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new FromlabelhighlightDirective();
    expect(directive).toBeTruthy();
  });
});
